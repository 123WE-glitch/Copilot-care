{
  "version": 2,
  "buildCommand": "npm install && npm run build --workspace=@copilot-care/frontend",
  "outputDirectory": "src/frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ]
}